use strict;
my $num_test_cases;
$num_test_cases = <STDIN>;
my $test_ct;
for ($test_ct = 1; $test_ct<=$num_test_cases;$test_ct++)
{
	my $k_num_coins;
	$k_num_coins = <STDIN>; 
	my $k_ct;
	for ($k_ct=1; $k_ct<=$k_num_coins; $k_ct++)
	{
		my ($x,$y) = <STDIN>;
	}
}
